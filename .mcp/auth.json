{
  "auth": {
    "method": "API_KEY",
    "apiKey": "YOUR_STITCH_API_KEY",
    "description": "Store your Stitch API credentials here. Alternatively, authenticate via Google Cloud ADC."
  },
  "alternativeMethods": {
    "googleCloudADC": {
      "command": "gcloud auth application-default login",
      "description": "Stitch MCP will automatically pick up credentials from Google Cloud Application Default Credentials if STITCH_API_KEY is not set."
    },
    "serviceAccount": {
      "env": "GOOGLE_APPLICATION_CREDENTIALS",
      "path": "path/to/service-account.json",
      "description": "Define the path to your Google Cloud Service Account JSON key."
    }
  }
}
